import { Fragment } from "react"
import Button from "../"

const Header = (props) => {
    return (
        <Fragment>
            <header>
                <h1>
                    Food Order
                </h1>
                <Button>

                </Button>
            </header>

            

        </Fragment>
    )
}

export default Header
