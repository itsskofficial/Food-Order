import { Fragment } from "react"

const Header = (props) => {
    return (
        <Fragment>
            <header>
                <h1>
                    Food Order
                </h1>
                <Button></button>
            </header>

            

        </Fragment>
    )
}

export default Header
